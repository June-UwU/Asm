#include <stdio.h>
#include <stdlib.h>

char* Reserve(const int& num)
{
	switch(*num)
	{
		case :
		{
			printf("it does\n");
		}
	}
}

int main(int argc, char const *argv[])
{
	int num;
	scanf("%d",&num);
	Reserve(num);
	return 0;
}